id = "json-repair"
version = "1"
kind = "repair"
compatibleAgents = ["repair-agent"]
inputSchemaRef = "failed-segment-artifact"
outputSchemaRef = "segment-script-draft"
contextRequirements = ["segment", "failed_artifact"]
toolAllowlist = []
promptBundle = ["prompts/system.md", "prompts/user.md"]
modelPolicy = "cheap-repair"
repairPolicy = "bounded-repair-depth"
successCriteria = ["returns-repaired-draft", "does-not-persist"]
telemetryTags = ["runtime", "repair"]
